import { styled } from '@mui/system';
import { Box } from '@mui/material';

const FlexColMain = styled(Box)({
    display: "flex",
    flexDirection:"column",
    alignItems: "center",
    boxShadow: "none",
})

export default FlexColMain;