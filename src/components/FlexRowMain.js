import { styled } from '@mui/system';
import { Box } from '@mui/material';

const FlexRowMain = styled(Box)({
    display: "flex",
    flexDirection:"row",
    alignItems: "center",
    boxShadow: "none",
})

export default FlexRowMain;